/* react */
import { memo } from 'react';

const CommerceMenuMigrater = () => {
    return <form onSubmit={event => event.preventDefault()}>CommerceMenuMigrater</form>;
};

export default memo(CommerceMenuMigrater);
